# HabitOS Frontend Environment Variables
# Copy this file to .env.local and fill in your actual values

# Backend API URL
REACT_APP_BACKEND_URL=http://localhost:3001

# Landing Page URL (where payments are processed)
REACT_APP_LANDING_PAGE_URL=https://your-landing-page.com/purchase

# ==================== IMPORTANT NOTES ====================
# 1. NEVER commit .env.local to version control
# 2. Use .env.local for local development
# 3. Use platform environment variables in production
# 4. The backend URL should point to your deployed backend in production
