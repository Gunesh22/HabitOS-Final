# HabitOS Backend Environment Variables
# Copy this file to .env and fill in your actual values

# Server Configuration
NODE_ENV=development
PORT=3001

# CORS Configuration (comma-separated list of allowed origins)
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:3001

# Gumroad Configuration
# Get your product ID from: https://app.gumroad.com/products
GUMROAD_PRODUCT_ID=your_product_id_here

# Security
# Generate a strong random string for JWT secret
JWT_SECRET=your_super_secret_jwt_key_here_change_this_in_production

# License Key Salt (for hashing)
# Generate a random string - NEVER change this after creating licenses!
LICENSE_SALT=your_random_salt_string_here_never_change_this

# Database Configuration (PostgreSQL)
# Local development example:
# DATABASE_URL=postgresql://username:password@localhost:5432/habitos
# Production (Render/Railway/Heroku will provide this):
DATABASE_URL=postgresql://localhost:5432/habitos

# Database (if you add database later)
# DATABASE_URL=your_database_connection_string_here

# ==================== IMPORTANT SECURITY NOTES ====================
# 1. NEVER commit the .env file to version control
# 2. NEVER share your API keys or secrets
# 3. Use different values for development and production
# 4. Rotate secrets regularly in production
# 5. Use platform environment variables in production (Render, Railway, etc.)
